'use strict';

/**
 * Founders Plot — legacy pure-engine snapshot (deprecated).
 *
 * Re-exports the current service-style engine so any lingering require()
 * resolves without importing stale implementations.
 */

module.exports = require('./engine');
