'use strict';

/**
 * Founders Plot — tool handlers (deprecated).
 *
 * The tool surface is now served directly by engine.js; routes.js registers
 * each HTTP endpoint against a named engine function. This file is kept as a
 * no-op export to preserve module resolution in downstream fixtures.
 */

module.exports = {};
