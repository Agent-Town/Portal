'use strict';

/**
 * Founders Plot — service bridge (deprecated).
 *
 * The active implementation now lives in engine.js, which talks directly
 * to the sqlite store. This file is retained as a no-op export to avoid
 * breaking any outside reference in test fixtures.
 */

module.exports = {};
