### Founders Plot Character Assets

`v1_4_4/` is the canonical Clover source for the app and for generated
inhabitant style references.

The root-level `clover-*.webp` files are older compatibility assets. New code
should prefer `v1_4_4/clover-*.webp` or `v1_4_4/clover-*.png`.
