// Placeholder Agent0 SDK bundle.
// Replace this file with a real `agent0-sdk` ESM bundle to enable ERC-8004 minting.
export const AG0_SDK_BUNDLED = false;

export class SDK {
  constructor() {
    throw new Error('AG0_SDK_NOT_BUNDLED');
  }
}
