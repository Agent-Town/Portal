# Agent Town registry source

This directory contains the source stubs and contracts referenced by the private `@agent-town` registry items.

These are intentionally minimal:
- enough to define install targets
- enough to document block responsibilities
- not a full production implementation
