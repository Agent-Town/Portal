# V6 Agent Civilization Milestone Plan

Status: `living_goal`

Source branch: `codex/v6-agent-civilization-milestones`

This document is the working ladder from the hardened V5 world-grid prototype
to V6.0 Agent Civilization completion. Keep it current as implementation lands:
when a milestone moves, update its status, evidence links, release gates, and
tests in the same change.

## Product Boundary

V6 is not "more autonomous agents everywhere." V6 is the first release where
humans and agents can participate in bounded, auditable civic institutions.

Until every prerequisite below is met:

- V6 remains research-only.
- No V6 civic mechanics are player-visible in normal gameplay.
- No public autonomous agent may mutate another user's world.
- No private Founders Plot state may leak into public civic surfaces.
- Human approval or explicit delegation is required for civic effects.

## Milestone Status Vocabulary

- `done`: implemented, tested, documented, and merged into the V6 baseline.
- `in_progress`: active work exists but the release gate is not closed.
- `planned`: specified enough to start but not yet implemented.
- `blocked`: known dependency or safety gap prevents implementation.
- `research_only`: allowed for internal docs/tests/prototypes only.

## V6 Ladder

| Milestone | Status | Purpose | Definition of Done |
| --- | --- | --- | --- |
| M0 Hardened V5 world-grid baseline | `in_progress` | Establish the safe V5.0-V5.5 prototype baseline that V6 can build on. | Public presence XSS is covered, V5.1+ mutations require an existing plot, all externally visible mutating prototype routes replay exact idempotent retries and reject changed key reuse, production mutations require same-origin context and owner-bound CSRF tokens with an optional durable hashed-token and session-binding SQLite foundation, browser coverage proves a token from one same-wallet session cannot mutate another same-wallet session, token issuance rotates old same-session tokens with explicit invalidation coverage, owner/surface rate limits are enforced with an optional durable SQLite foundation, feature-gated tools have parity tests, V6 is still hidden, and split V5 Playwright coverage passes. |
| M1 Living V6 milestone contract | `done` | Keep the V6 path explicit and refine it as progress lands. | This document, `specs/54_agent_town_v6_agent_civilization_foundation.md`, `specs/release-gates/v60_agent_civilization_readiness_gate.md`, and `server/world_civilization/readiness_gate.js` cross-link the milestone ladder, readiness evidence, and aggregate gate contract; tests prove the major milestones remain named. |
| M2 V5 evidence promotion gates | `in_progress` | Promote V5.0-V5.5 from prototype evidence to release-grade prerequisites. | Each V5 slice has owner/auth checks, restart persistence tests, durable audit/replay evidence beyond the current process-local idempotency guard, route/tool audit matrix proof, security notes, deterministic Node + Playwright coverage, same-session player-route prerequisite proof in `e2e/245_world_grid_player_route_prerequisite.spec.js`, and `specs/release-gates/v5_world_grid_release_promotion_gate.md` coverage. |
| M3 Release-grade world storage | `in_progress` | Replace process-local world-grid stores before public civic systems depend on them. | Durable owner indexes, migration versioning, idempotency records, audit/replay rows, restart persistence tests, rollback handles, the `WORLD_GRID_AUDIT_SQLITE_PATH` durable world-grid audit log foundation with V5.1-V5.5 mutating route/tool-surface restart matrix proof, duplicate-replay suppression, and privacy-safe `agent-town.v5.world-grid.audit-snapshot.v1` before/after route snapshot proof with public presence, services, events, and sandbox aggregate summaries, the `WORLD_GRID_CSRF_SQLITE_PATH` durable owner-bound hashed CSRF token and hashed session-binding foundation with expiry, cross-session denial, production route restart proof, browser same-wallet cross-session denial proof, same-session token rotation, explicit invalidation proof, session-reset invalidation wiring, wallet/provider disconnect invalidation endpoint plus client hook, and mocked provider disconnect callback proof, the `WORLD_GRID_RATE_LIMIT_SQLITE_PATH` durable owner/surface mutation rate-limit foundation with route restart proof, the `WORLD_GRID_REGION_PREFS_SQLITE_PATH` durable V5.0 camera/focus preference foundation with owner-indexed restart proof, the `WORLD_GRID_IDEMPOTENCY_SQLITE_PATH` durable idempotency foundation with V5.1-V5.5 mutating route/tool-surface restart replay proof, the `WORLD_GRID_CLAIMS_SQLITE_PATH` durable V5.1 claims foundation with planned/claimed/cancel restart proof and cross-owner route-mutation denial, the `WORLD_GRID_PUBLIC_PRESENCE_SQLITE_PATH` durable V5.2 public presence/follow/abuse-report foundation with restart proof and private-text redaction, the `WORLD_GRID_SERVICES_SQLITE_PATH` durable V5.3 service request/reputation foundation with restart proof, the `WORLD_GRID_EVENTS_SQLITE_PATH` durable V5.4 event contribution/reward foundation with restart proof, the `WORLD_GRID_SANDBOX_SQLITE_PATH` durable V5.5 sandbox participant/action/snapshot/cell foundation with restart proof, and the `server/world_civilization/audit_ledger.js` foundation exist for every mutating world endpoint; final session-auth integration, live Privy/provider logout signoff, IP/risk-aware production sharing, complete exact per-record before-state reconstruction, and production replay coverage remain release gates. |
| M4 Civic schema contracts | `done` | Define the stable data contracts for V6 institutions. | Proposal, vote, delegation, institution charter, civic action, reputation, reputation dispute/review, moderation decision, moderation review/appeal, rollback, and audit ledger schemas are documented in `specs/55_agent_town_v6_civic_schema_contracts.md` and validated by `tests/world_civilization_schemas.test.js`. |
| M5 Mutation security controls | `in_progress` | Make civic mutations safe against cross-origin, replay, and abuse paths. | `server/world_civilization/mutation_security.js` defines a research-only fail-closed civic mutation security envelope for future V6 routes/tools covering explicit V6 opt-in, same-origin checks, session/wallet auth, actor/owner binding, store-backed delegated-agent proof with required scope and remaining budget evidence, exact delegated-action replay allowance for an already consumed delegation/idempotency key, CSRF verification, idempotency keys, owner/surface rate limits, and hidden non-executing status; `server/world_civilization/session_auth_targets.js` and `tests/world_civilization_session_auth_targets.test.js` now define the M5 release target matrix for session/wallet binding, session-bound CSRF, delegated-principal binding, provider-disconnect invalidation, session-reset invalidation, route/tool middleware integration, production browser session coverage, risk-aware rate-limit identity, audit actor continuity, and private-data exclusion while keeping `releaseReady: false`; V5 now has optional durable owner-bound/session-bound CSRF, browser same-wallet cross-session CSRF denial coverage in `e2e/243_world_grid_csrf_session_binding.spec.js`, same-session token rotation plus explicit invalidation coverage in `tests/world_grid_csrf_persistence.test.js`, session-reset invalidation wiring in `/api/session/reset`, wallet/provider disconnect invalidation wiring in `/api/session/world-grid-csrf/invalidate` plus `public/app.js`, mocked provider disconnect callback proof, and owner/surface rate-limit foundations for world-grid mutations. M5 still needs final session-auth middleware integration, live Privy/provider logout signoff, final route/tool middleware integration, production browser coverage, IP/risk-aware production rate-limit sharing, audit actor-continuity release review, and release signoff before `done`. |
| M6 Worker-first V6 tool surface | `in_progress` | Preserve the OpenClaw Lite worker as the authority for agent behavior. | `FEATURE_WORLD_V60_AGENT_CIVILIZATION` defaults off, broad V5 prototype overrides do not enable V6, production player `worldGridFeatureFlags=all,v60` query/header overrides cannot enable V6 when V5 is server-enabled, a server-side V6 flag still does not publish `et.world.civic.*` tools through runtime `/api/world/tools`, `server/world_civilization/tools.js` defines a research-only non-executing civic tool draft, `server/world_civilization/worker_tool_adapter.js` adds an internal OpenClaw Lite worker-origin adapter for `et.world.civic.proposals.submit_for_review` that requires `V6_CIVIC_WORKER_TOOL_ADAPTER_ENABLED`, Worker Tools/Skill Context/Worker Traffic/Brain/Session Context observability, store-backed `proposal_drafting` delegation, M5 mutation security, idempotent delegated action-budget consumption, and no runtime civic tool exposure, `server/world_civilization/worker_vote_adapter.js` adds an internal OpenClaw Lite worker-origin adapter for `et.world.civic.votes.cast` through `castVoteFromWorkerTool()` that requires `V6_CIVIC_WORKER_VOTE_ADAPTER_ENABLED`, full worker observability, store-backed `vote_advice` delegation, same-origin/CSRF-reviewed M5 mutation security, the M8 route-edge authorization envelope, vote receipt idempotency, idempotent delegated action-budget consumption, and no vote outcome application, `server/world_civilization/tool_exposure_gate.js` defines a fail-closed exposure gate requiring `/api/world/tools` as runtime source of truth, OpenClaw Lite worker origin, Worker Tools/Skill Context/Worker Traffic/Brain/Session Context observability, mutation-security evidence including store-backed delegated-agent proof, delegation scope mismatch, read-only delegation budget handling, and no public runtime `et.world.civic.*` tools, and `server/world_civilization/worker_runtime_registration.js` with `tests/world_civilization_worker_runtime_registration.test.js` defines the browser worker runtime registration target matrix for worker boot, manifest sync, civic-tool absence, debug observability, skill context, worker traffic, session context, modal continuity, shared-state route adapters, and production override denial while keeping runtime civic tools unregistered. M6 still needs actual browser worker/runtime registration, production browser coverage, and release signoff before any callable V6 civic tool can exist in normal runtime manifests. |
| M7 Internal proposal lifecycle | `in_progress` | Let humans or agents draft bounded civic proposals without executing them. | Proposals have scope, proposer identity, effect preview, moderation class, expiry, idempotency, private-data redaction, audit records, and no state mutation until approved; proposal draft/review audit entries now include privacy-safe before/after summaries instead of hash-only fallback text; `recordProposalReview()` validates moderation decisions, transitions approved reviews to `ready_for_vote`, transitions rejected reviews to `rejected`, writes `proposal.reviewed` audit entries, and still exposes no apply/execute path; `server/world_civilization/governance_preflight.js` now requires an existing, active, review-ready proposal before any prepared effect can be recorded; `buildV6ProposalSubmissionEnvelope()` validates research-only human route and worker-tool submission envelopes against explicit V6 opt-in, approval receipt, proposal schema, M5 mutation-security envelope, actor/proposer binding, OpenClaw Lite worker origin for worker tools, no runtime exposure, and no effect execution; `submitProposalForReview()` uses that envelope to create a drafted review-queue proposal without public runtime tools or civic effects; `server/world_civilization/routes.js` now mounts research-only `POST /api/world/civilization/proposals/submit`, defaults it disabled behind `V6_CIVIC_PROPOSAL_SUBMISSION_ROUTE_ENABLED`, composes env-gated SQLite proposal/audit/delegation store wiring from `server/world_civilization/store_wiring.js` only when `V6_CIVIC_PROPOSAL_STORE_WIRING_ENABLED` and the required SQLite paths are present, requires explicit V6 flag opt-in, fails closed without same-origin/CSRF/session/wallet/delegation evidence, consumes `proposal_drafting` delegated action budget idempotently for hidden worker-tool route receipts, persists nothing on denial, and survives store reopen while remaining `releaseReady: false`; `server/world_civilization/worker_tool_adapter.js` now submits an agent-authored proposal to the same review queue through an internal worker-origin adapter for `et.world.civic.proposals.submit_for_review`, requiring OpenClaw Lite observability, store-backed delegation, same-origin/CSRF-reviewed M5 security, idempotency, exact delegated action-budget consumption for successful receipts, no backend shortcut, and no runtime civic tool exposure; `getProposalReviewQueueSnapshot()` provides a research-only internal review-queue snapshot for live drafted proposals awaiting moderation, excludes reviewed and expired proposals by default, survives reopen, and exposes no proposal payload, runtime tool, player surface, civic tool, private data, or effect execution; `buildV6ProposalIntakeReadinessGate()` records the non-executing M7 proposal intake readiness gate over human route submission, worker tool submission, submission envelope, approval receipt binding, proposal-submission mutation security, worker-tool origin enforcement, Skill Context and Worker Traffic observability, same-origin/CSRF/session auth, idempotent submission replay, review-queue index, review-queue snapshot, reviewed/expired queue exclusion, moderation decision link, proposal audit rows, public-text privacy review, private-data exclusion, no backend shortcuts, no civic tool exposure, and no effect execution. M7 still needs browser worker registration, release review for route-store operations, production browser coverage, and security/product signoff before `done`. |
| M8 Vote authorization and delegation | `in_progress` | Add explicit consent mechanics for civic decisions. | Vote auth prevents forgery/replay, enforces eligibility, records receipts, handles abstain/revoke/delegation policy, proves one-vote accounting, starts with `server/world_civilization/votes.js`, now includes non-executing `evaluateVoteApprovalPolicy()` coverage for `quorumMinVotes`, `minApproveVotes`, and `approvalThresholdBps`, writes `vote.recorded` audit rows with privacy-safe before/after summaries, and adds `buildV6VoteRouteAuthorizationEnvelope()` as a non-recording route-edge guard that composes the V6 feature gate, explicit research opt-in, M5 mutation-security envelope, review-ready proposal state, human/delegated/worker vote-surface binding, voter authorization, eligibility, and no effect application; `server/world_civilization/routes.js` now mounts disabled-by-default research-only `POST /api/world/civilization/votes/cast` behind `V6_CIVIC_VOTE_ROUTE_ENABLED`, records only authorized human/delegated-agent vote receipts through the route-edge envelope, keeps `worker_tool_vote_surface` reserved from HTTP route calls, fails closed without same-origin/CSRF/session/wallet/delegation evidence, consumes `vote_advice` delegated action budget idempotently for hidden delegated-agent route receipts, and can use env-gated SQLite vote store wiring from `server/world_civilization/store_wiring.js` only behind `V6_CIVIC_VOTE_STORE_WIRING_ENABLED`, `V6_CIVIC_AUDIT_SQLITE_PATH`, `V6_CIVIC_PROPOSAL_SQLITE_PATH`, and `V6_CIVIC_VOTE_SQLITE_PATH` while remaining `releaseReady: false`; `server/world_civilization/worker_vote_adapter.js` adds disabled-by-default worker-tool vote registration for `et.world.civic.votes.cast` through `castVoteFromWorkerTool()`, requiring `V6_CIVIC_WORKER_VOTE_ADAPTER_ENABLED`, OpenClaw Lite worker origin, full debug observability, store-backed `vote_advice` delegation, same-origin/CSRF-reviewed M5 mutation security, the worker route-edge vote authorization envelope, receipt idempotency, exact delegated action-budget consumption for successful receipts, no runtime civic tool exposure, and no outcome application; `server/world_civilization/voting_templates.js` adds research-only per-institution voting templates for public-world, public-works, sandbox-policy, institution-charter, and service-policy scopes plus `buildV6VotingTemplateReviewReport()` coverage; `buildV6VoteAuthorizationReadinessGate()` records a non-executing M8 readiness gate for server-verified voter authorization, eligibility rules, one-vote accounting, idempotent receipt replay, changed-vote replay rejection, proposal expiry denial, delegation policy review, per-institution voting templates, route-edge vote auth, worker-tool vote registration, quorum/threshold policy, governance-preflight integration, vote audit rows, private-data exclusion, and no effect application. M8 still needs release signoff for voting templates, browser worker/runtime registration, production browser coverage, and security/product signoff before `done`. |
| M9 Reputation and accountability | `in_progress` | Add bounded trust signals without turning reputation into farmable currency. | `server/world_civilization/reputation.js` stores durable research-only reputation records plus dispute/review records with audit entries, duplicate-source protection, self-award rejection, private-data rejection, human dispute requesters, non-transferable summaries, dispute/review replay indexes, and privacy-safe before/after audit summaries for reputation/dispute records; when wired to `server/world_civilization/moderation.js`, reputation disputes can require an existing moderation decision linked to the reputation record source before persistence, with separate Node restart coverage. `buildV6ReputationEligibilityAdviceGate()` now records the non-executing M9 eligibility/advice readiness gate over reviewed eligibility policy, advice policy, reputation source-kind coverage, moderation/dispute linkage, privacy/product review, public-text rendering, private-data exclusion, no score mutation, no agent authority grant, no farmable currency, no runtime exposure, and no player-visible reputation. M9 still needs real production eligibility/advice integration and privacy/product signoff before `done`. |
| M10 Moderation and privacy layer | `in_progress` | Moderate public civic text, profiles, agent content, media, and public effects. | `server/world_civilization/moderation.js` stores durable research-only moderation decisions plus human review/appeal records with private-data rejection, redacted-field evidence, canonical subject/policy decisions, abuse-report source references, reviewer/status/review-queue replay indexes, privacy-safe before/after audit summaries, and non-executable summaries; `buildV6ModerationPrivacyReadinessGate()` now records the non-executing M10 moderation privacy readiness gate over proposal text, agent-authored content, public profile, attached media, sandbox artifact, public works effect, abuse-report triage, appeal operations, human review tooling, redaction, public-text rendering, public-presence privacy, private-data exclusion, review replay, moderation/appeal audit row, no moderation effect application, no content publication, no runtime exposure, and no player-visible moderation evidence. M10 still needs production review tooling, appeals operations, media review, and full public-surface integration before `done`. |
| M11 Civic effect execution and rollback | `in_progress` | Execute approved civic effects safely and reversibly. | `server/world_civilization/effects.js` stores research-only prepared civic effects and rollback handles after `server/world_civilization/governance_preflight.js` verifies proposal existence, proposal review-ready state, approved moderation, vote approval policy, human approval receipt, effect-type, and rollback-plan prerequisites; prepared-effect audit rows include privacy-safe before/after summaries, rollback id, and no-world-state-applied evidence with zero hash-only fallback proof across process restart; `server/world_civilization/schemas.js` now enforces a schema-level typed effect handler registry so civic actions cannot pair an effect with the wrong future handler; `buildV6CivicEffectExecutionGate()` records a non-executing M11 readiness gate for typed apply handlers, typed rollback handlers, real before/after state, authorization, idempotent apply/rollback, irreversible-action review, conservation tests, applied/rollback audit rows, and worker/route security; `server/world_civilization/rollback_recovery.js` reconstructs prepared rollback handles from reopened effect/audit stores without executing state; `server/world_civilization/rollback_execution_targets.js` and `tests/world_civilization_rollback_execution_targets.test.js` define the typed rollback execution target matrix for every current effect type while rejecting executable handlers, execution drills, private row payloads, fake release readiness, and world-state application. M11 still needs actual executable handlers, applied/rollback states, and release-signed conservation/rollback execution before `done`. |
| M12 Agent participation controls | `in_progress` | Allow agents to participate without silent authority escalation. | `server/world_civilization/delegations.js` stores research-only scoped delegations with expiry, action limits, approval receipts, idempotent action-budget consumption, revocation, audit entries with privacy-safe lifecycle before/after summaries, replay indexes, and non-executable participation summaries; delegation restart replay now proves zero hash-only fallback summaries for creation, action-budget consumption, and revocation; `server/world_civilization/mutation_security.js` now recognizes exact delegated-action replay by delegation/idempotency key after the action budget reaches zero while still denying new actions; `server/world_civilization/routes.js`, `server/world_civilization/worker_tool_adapter.js`, and `server/world_civilization/worker_vote_adapter.js` consume scoped delegated action budget exactly once for successful hidden research proposal/vote receipts while preserving exact replay; `server/world_civilization/governance_preflight.js` requires read-only active `civic_execution` delegation proof and treats the old `allowDelegatedExecution` switch as insufficient by itself; `buildV6AgentParticipationEnforcementGate()` records a non-executing M12 readiness gate for worker-tool scope enforcement, route-edge scope/expiry/budget/revocation checks, principal wallet/session binding, idempotent budget consumption, store-backed delegation proof, audit rows, no backend shortcuts, and no public autonomous mutation. M12 still needs executable delegated-effect integration after M11 release gates close, browser worker/runtime signoff, and security/product signoff before `done`. |
| M13 Civic institutions and charters | `in_progress` | Define the first player-visible civic structures. | `server/world_civilization/institutions.js` stores research-only public-safe institution charters plus proposal/vote/moderation-gated charter amendment records with human chartering/requesting actors, scope boundaries, membership/eligibility rules, moderation policy, proposal types, voting rules, audit entries with privacy-safe before/after charter summaries, replay indexes, and non-executable summaries; institution restart replay now proves zero hash-only fallback summaries for charter and amendment rows; `buildV6CivicInstitutionReadinessGate()` records a non-executing M13 readiness gate for release-reviewed charter, membership, eligibility, voting, moderation, proposal-type, public-audit, public-text, delegation-policy, charter-change execution/rollback, private-data exclusion, and institution-audit evidence across public-world, public-works, sandbox-policy, institution-charter, and service-policy templates. M13 still needs real worker/tool integration, applied charter-change execution, public surfaces, and release signoff before `done`. |
| M14 Public works and shared resources integration | `in_progress` | Connect V6 institutions to V5.4-style public works without unsafe free play. | `server/world_civilization/public_works.js` stores research-only proposal/vote/moderation-gated public works project records plus institution-scoped contributions with per-contribution/per-contributor/project caps, accepted-vs-capped bundles, audit entries with privacy-safe before/after bundle summaries, replay indexes, and conservation summaries that do not mutate private inventory or grant rewards; public-works restart replay now proves zero hash-only fallback summaries for project/contribution rows; `buildV6PublicWorksReadinessGate()` records a non-executing M14 readiness gate for governed project review, worker/tool enforcement, wallet/session route auth, durable idempotency, explicit inventory-spend authorization, inventory restart replay, resource conservation tests, reward conservation, contribution caps under retry, rollback execution review, public text rendering, private-data exclusion, public-works audit rows, process restart replay, no private-town mutation, and no public free play. M14 still needs real worker/tool integration, authorized private-inventory spend, reward/rollback execution, public surfaces, and release signoff before `done`. |
| M15 Modal-first V6 lab surface | `in_progress` | Expose V6 work through a safe internal modal flow before release. | `server/world_civilization/lab_surface.js` defines a route-neutral, feature-gated, non-executing lab surface contract plus a fail-closed modal launch plan that denies standalone `/v6`, `/v6-lab`, and `/civilization` paths, requires the `/app` town hub modal, preserves worker continuity, and requires Worker Tools, Skill Context, Worker Traffic, Brain, and Session Context observability; the Express route layer now redirects standalone lab paths to `/app` and exposes `GET /api/world/civilization/lab/launch-plan` only behind `V6_CIVIC_LAB_MODAL_ENABLED`, explicit `v6Lab=1` opt-in, the V6 feature flag, town-hub route context, and production admin/QA override authorization; `public/app.js` renders the lab through the existing district modal using DOM-created, non-executing panels only after the launch plan allows it, while normal `/app` gameplay exposes no V6 lab markers or `et.world.civic.*` tools by default; `e2e/244_v6_lab_modal_boundary.spec.js` proves standalone redirects, normal gameplay denial, internal modal launch, keyboard focus containment, screenshot evidence at 390/768/1280 widths, and runtime tool absence; `buildV6LabReadinessGate()` records a non-executing M15 readiness gate for town-hub modal launch, standalone route denial, worker continuity, debug observability, non-executing panels, browser visual coverage at 390/768/1280 widths, keyboard accessibility, focus trap review, screen-reader names, runtime tool absence, private debug-data exclusion, and normal gameplay exposure denial. M15 still needs production release review and release signoff before `done`. |
| M16 Persistence, replay, and resilience hardening | `in_progress` | Prove V6 survives restarts and failure modes. | `server/world_civilization/resilience.js` defines a research-only baseline report over the current SQLite civic stores plus `buildV6ResilienceReadinessGate()` for a non-executing M16 readiness gate, `server/world_civilization/sqlite_schema.js` stamps v1 on-disk schema metadata with fail-closed drift checks, `server/world_civilization/replay_reconstruction.js` reconstructs privacy-safe audit summaries without applying effects, now requires before/after audit summary presence, and tracks hash-only fallbacks as research evidence rather than release-grade store reconstruction, `V6_CIVIC_AUDIT_SUMMARY_COVERAGE` aggregates store-specific zero hash-only fallback proof across the current proposal/vote, reputation/moderation, effect, delegation, institution, and public-works restart probes, `server/world_civilization/load_rate_targets.js` defines research-only release SLO surfaces for audit replay, duplicate retry bursts, conflict rejection, migration-load replay, write contention, and future civic route rate limits while `tests/world_civilization_load_rate_targets.test.js` proves the target matrix remains calibration-only, `releaseReady: false`, non-executing, private-row-free, and non-world-mutating, `server/world_civilization/migration_rehearsal.js` inventories current v1 store metadata while `tests/world_civilization_migration_rehearsal.test.js` proves unsupported upgrade/downgrade targets fail closed, `server/world_civilization/migration_load_replay.js` records a research-only migration-load replay rehearsal while `tests/world_civilization_migration_load_replay.test.js` proves current v1 schema inventory can be paired with bounded privacy-safe audit replay without executing migration scripts, exposing row payloads, or applying world state, `server/world_civilization/backup_restore.js` now records a research-only closed-store SQLite backup/restore rehearsal with source/restored hash matching, restored schema metadata, private row-payload exclusion, and no world-state application coverage in `tests/world_civilization_backup_restore.test.js`, `server/world_civilization/write_contention.js` records a research-only multi-process write-contention rehearsal while `tests/world_civilization_write_contention.test.js` proves concurrent audit-ledger writers serialize through `BEGIN IMMEDIATE`, preserve hash-chain integrity, suppress exact duplicate retries, exclude row payloads from the report, and apply no world state, process restart probes now cover audit-ledger, proposal/vote, reputation record/dispute, moderation decision/review, effect/rollback, delegation, institution, and public-works replay across separate Node lifetimes, `tests/world_civilization_load_rate.test.js` covers research-scale replay pagination plus duplicate retry bursts, `tests/world_civilization_rollback_recovery.test.js` covers non-executing prepared rollback-handle reconstruction after reopen, and `tests/world_civilization_rollback_execution_targets.test.js` covers typed rollback execution target mapping without executable handlers. The readiness gate records restart, replay, hash-chain, privacy, store-specific zero hash-only fallback proof, migration upgrade/downgrade scripts, migration-load replay, backup/restore, write contention, production load/rate, rollback handle reconstruction, and typed rollback execution recovery evidence while keeping `releaseReady: false`. M16 keeps `releaseReady: false` until release-grade replay reconstruction, encrypted/point-in-time backup restore drills, production load/rate and write-contention route/store tests, real rollback recovery execution, and migration upgrade/downgrade scripts pass. |
| M17 Security and product release review | `in_progress` | Close the release gate before normal gameplay exposure. | `server/world_civilization/release_review.js` defines a research-only release-review gate covering threat model, privacy review, abuse-case review, M5 session-auth target gate evidence, store-backed delegation proof and scope-mismatch evidence, data-retention policy, audit coverage with store-specific audit-summary coverage, Playwright/Node validation, proposal intake readiness review, reputation eligibility/advice readiness review, moderation privacy readiness review, effect execution and rollback review, agent participation enforcement review, civic institution readiness review, public works readiness review, worker tool surface review, modal lab surface review, resilience readiness review with store-specific zero hash-only fallback proof, and product signoff; `server/world_civilization/privacy_review_targets.js` and `tests/world_civilization_privacy_review_targets.test.js` now define the privacy review target matrix for private town isolation, public surface minimization, wallet/Brain/provider secret exclusion, debug/worker trace redaction, public text rendering/XSS, modal lab private-data exclusion, audit summary minimization, and cross-account boundaries while staying non-executing and `releaseReady: false`; `server/world_civilization/data_retention_targets.js` and `tests/world_civilization_data_retention_targets.test.js` now define the data-retention target matrix for audit-ledger retention, proposal/vote records, reputation/moderation records, delegation authority records, worker debug traces, subject export/deletion boundaries, private credential exclusion, backup retention, and retention-aware audit replay while staying non-executing and `releaseReady: false`; validation evidence now includes the M17 privacy review target gate, the M5 session-auth target gate, the M17 data-retention target gate, the M7 proposal intake readiness gate, M9 reputation eligibility advice gate, M10 moderation privacy readiness gate, lab readiness gate, and M16 resilience readiness gate contract, including `store_specific_zero_hash_only_fallbacks`, and V6 remains blocked until all evidence and signoffs are complete. |
| M18 V6 controlled release completion | `in_progress` | Make V6 player-visible only after all gates are closed. | `server/world_civilization/readiness_gate.js` defines `buildV6ReadinessGateReport()` as the research-only aggregate V6 readiness gate across V5 promotion, feature flag safety, schemas, audit replay, mutation security, worker tools, proposal/vote governance, reputation/moderation privacy, rollback, agent participation, institutions/public works, modal lab, persistence resilience with `store_specific_zero_hash_only_fallbacks`, and release review with `store_specific_audit_summary_coverage`; `server/world_civilization/controlled_release.js` defines a research-only controlled-release gate that requires M0-M17 completion, release-review readiness, an explicit closed V6 readiness-gate report that remains hidden until controlled release, readiness audit-summary proof, production-safe flags, rollback/disable controls, observability, support runbooks, blocker clearance, and a controlled release window before any explicit production enablement. |

## Immediate Working Order

1. Keep M0/M1 green while the hardening branch is reviewed.
2. Close M2 by promoting V5 prototype evidence into release-grade gates.
3. Close M3-M6 before expanding V6 civic mechanics beyond research-only
   contracts/foundations.
4. Keep the M6 civic tool draft non-executing and hidden until the tool
   exposure gate proves worker/runtime exposure, observability, and
   mutating-route security controls are complete.
5. Keep M7-M12 behind research/internal flags only until V5 hardening gates are
   closed.
6. Add M13-M15 once schemas, auth, moderation, privacy, and rollback are proven.
7. Treat M16-M18 as release hardening, not prototype discovery.

## Completion Rule

V6 is complete only when M0-M18 are `done`, the V6 readiness gate is closed,
and the normal player flow can enable V6 without hidden mutation, private-data
leakage, unaudited civic effects, or public autonomous agents acting outside
explicit human approval or delegation.
